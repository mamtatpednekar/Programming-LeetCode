class Solution(object):
    def xorOperation(self, n, start):
        """
        :type n: int
        :type start: int
        :rtype: int
        """
        res = 0
        myList = []
        for i in range(0, n):
            myList.append(start + 2 * i)
            res ^=myList[i]
        return res
