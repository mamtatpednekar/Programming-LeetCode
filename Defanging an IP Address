class Solution(object):
    def defangIPaddr(self, address):
        """
        :type address: str
        :rtype: str
        """
        output = ""
        i = 0
        while i < len(address):
            if address[i] == ".":
                output = address.replace(".", "[.]")
            i += 1
            
        return output
    
    
    
