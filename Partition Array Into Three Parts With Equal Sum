count = 0
        output = 0
        sumall = int(sum(arr)/3)
        for i in range(0, len(arr)):
            output += arr[i]
            if output == sumall:
                count += 1
                output = 0
        if count == 3:
            return True
        else:
            return False
