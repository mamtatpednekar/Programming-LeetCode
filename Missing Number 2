class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        total = sum(range(0, len(nums)+1))
        return total - sum(nums)

        
