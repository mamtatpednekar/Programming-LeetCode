class Solution(object):
    def findDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        duplicate = set()
        for i in nums:
            if i in duplicate:
                return i 
            duplicate.add(i)
        
